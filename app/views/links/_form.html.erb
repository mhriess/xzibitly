<%= form_for(@link) do |f| %>
  <% if @link.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@link.errors.count, "error") %> prohibited this article from being saved:</h2>

        <ul>
        <% @link.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title, class: 'span5' %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url, class: 'span10' %>
  </div>
  <div class="field">
    <%= f.label :vanity_link %><br />
    <%= f.text_field :vanity_link, class: 'span10' %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn' %>
  </div>
<% end %>
